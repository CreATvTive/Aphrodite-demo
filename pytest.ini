[pytest]
markers =
    acceptance: contract acceptance evidence retained in the default full suite
    slow: intentionally long-running acceptance or soak evidence
    real_time: wall-clock acceptance that must not use a simulated clock
